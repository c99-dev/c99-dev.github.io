# 프로젝트 리뷰 · 2026-09-07

검토 범위: src의 컴포넌트·훅·CSS, public 데이터와 메타데이터, scripts, package.json/lockfile, GitHub Pages 배포 경로, 실제 배포 화면과 로컬 브라우저 동작.

## 발견한 문제와 반영한 개선

| 우선순위 | 발견한 문제 | 조치 |
| --- | --- | --- |
| P1 | 밴으로 가용 챔피언이 줄면 기대 인원에 도달할 수 없어 자동 추첨이 반복될 수 있음 | 팀당 인원을 가용 풀의 절반으로 제한하고 밴 해제 시 복원. 현재 선택된 밴 챔피언도 제거 |
| P1 | 랭킹만 실패했을 때 null인 gameDataError.message를 읽어 오류 화면 자체가 깨짐 | 챔피언 실패는 재시도 화면, 통계 실패는 안내와 함께 추첨 기능 유지 |
| P1 | 수집 스크립트가 Git에서 제외되고 랭킹 요청을 무한 재시도함. 이미지 실패를 성공으로 출력 | 필요한 스크립트를 버전 관리에 포함하고 타임아웃·제한된 재시도·PNG 검증·실패 종료 적용 |
| P1 | 이미지가 일부만 준비되어도 일정 시간 뒤 복사를 허용함 | 화면에 필요한 이미지만 준비하고 실제 변환 성공 여부로 복사 가능 상태 결정 |
| P2 | 최소 너비 1,000px와 고정 사이드 메뉴가 작은 화면을 가림 | 상단 도구 모음, 모바일 세로 팀 배치, 320px부터 가로 넘침 없는 레이아웃 |
| P2 | 색상·그림자·이모지 강조가 분산되고 서비스 제목과 핵심 동작의 구분이 약함 | 소개 영역 없이 간결한 도구 모음과 32px 챔피언 행으로 구성. 블루·레드 팀 구분과 작은 아이콘만 유지 |
| P2 | 클립보드 텍스트 순서가 화면 정렬과 다름 | 공통 정렬 함수 사용. 이미지 복사는 복제 DOM에서 고정 폭으로 생성 |
| P2 | 밴 버튼이 requestIdleCallback을 직접 호출하고 저장소 접근 오류를 처리하지 않음 | 중복 저장 경로 제거, localStorage 차단·오염 값에 대한 기본값 처리 |
| P2 | 리롤 확인 취소 시 Promise가 종료되지 않고 Enter가 취소 버튼에서도 확인으로 처리될 수 있음 | 확인·취소 시 Promise 종료, 실제 버튼의 기본 키보드 동작 사용 |
| P2 | 모달들이 body 스크롤 상태를 서로 덮어쓰고 배경 접근성 숨김을 비활성화함 | react-modal의 스크롤 잠금·포커스 관리·대화상자 이름을 사용 |
| P2 | 옵션을 저장하지 않고 닫아도 임시 값이 남음. 인원 소수를 허용함 | 모달을 열 때 저장된 값 복원, 정수 인원만 저장 |
| P2 | 챔피언·밴 선택이 마우스 전용 요소이고 순위·승률 옵션이 실제 표에 연결되지 않음 | 이름 있는 button, 밴 aria-pressed, 포커스 표시, 순위·승률 옵션 연결 |
| P3 | 공지/업데이트 재조회 성공 후에도 이전 오류가 남음. manifest에 잘못된 PNG 선언 | 조회 시작 시 오류 초기화, 실제 ICO 선언 및 테마 색상 정리 |

## 데이터 검증

- Riot 한국 서버 realm: Data Dragon 16.17.1, 게임 패치 26.17.
- championData.json: 173명. championRanking.json: 173명.
- 173개 챔피언 PNG 확인. 신규 Locke 이미지 추가, Shyvana 이미지 갱신.
- lol.ps 통계의 중첩 duoChampionIds 참조를 실제 ID로 복원.
- 게임 데이터는 배포된 JSON을 읽도록 변경하여 초기 로딩의 외부 CDN 의존성 제거.
- 작업 시작 시 이미 수정되어 있던 championData.json, championRanking.json, version.json 및 기존 scripts는 로컬 임시 백업에 보존한 뒤 갱신.

공식 확인: [26.17 패치 노트](https://www.leagueoflegends.com/ko-kr/news/game-updates/league-of-legends-patch-26-17-notes/), [한국 서버 realm](https://ddragon.leagueoflegends.com/realms/kr.json).

## 검증 결과

- React/Jest 회귀 테스트 20개 통과.
- Python 수집기 테스트 3개 통과: 다운로드 실패 시 기존 데이터 보존, 중첩 참조 복원, 빈 통계 거부.
- ESLint 오류·경고 0개, production 빌드 성공, git diff --check 통과.
- 실제 Chrome에서 인원 변경, 정렬, 텍스트 복사 순서, 리롤 확인·취소, 밴 검색·제외, 이미지 복사 확인.
- 데스크톱 및 390px/320px 모바일 레이아웃 확인. 최종 컴팩트 화면은 1280 × 720에서 두 팀 15명씩 모두 스크롤 없이 표시됨 (팀 패널 하단 662px).
- 기본 설정에서 팀당 15명을 640 × 566px PNG로 복사하는 것을 확인. 모바일에서도 동일한 가로 2열 캡처를 사용.
- Safari/Firefox의 실제 기기 검증은 수행하지 않음. 클립보드 미지원·권한 거부 시 안내 경로 제공.

## 남은 개선 우선순위

1. **P1 · CRA 빌드 도구 교체**: npm audit fix --ignore-scripts로 57개 → 31개, Critical 2개 → 0개. 남은 항목은 High 14 / Moderate 8 / Low 9이며 react-scripts의 빌드·개발 서버·테스트 의존성 경로에 연결됨. 정적 배포 파일의 실행 취약점 31개를 뜻하지는 않지만 개발·빌드 환경 위험은 남음. 강제 fix는 react-scripts 0.0.0으로의 파괴적 변경을 제안하므로 적용하지 않음. 별도 변경에서 지원되는 빌드 도구로 이전하고 배포와 캡처 회귀를 검증할 것.
2. **P2 · 자동 검증 연결**: 추가된 lint, React/Python 테스트, 데이터 검증을 CI에 연결하면 이후 패치 갱신 실패를 배포 전에 발견할 수 있음. 이번 작업에서는 예약 갱신이나 별도 자동화는 생성하지 않음.
3. **P2 · 더 넓은 접근성/브라우저 검증**: iOS Safari의 실제 클립보드 권한·키보드·VoiceOver 검증, 확대 배율과 강제 색상 모드 점검 권장.
4. **P3 · 남은 유지보수 정리**: useRandomChampions의 긴 리롤 결과 JSX를 별도 컴포넌트로 분리하고, 사용하지 않는 성능 측정 코드·라이브러리·중복 모달 CSS를 후속 작업에서 정리 가능. 이번 배포에는 불필요한 전면 리팩터링을 포함하지 않음.

현재 추첨은 중복 없는 무작위 추첨이며 팀 전력 균형화 기능은 없습니다. 팀 밸런싱은 별도 제품 선택이므로 이번 작업에서 임의 도입하지 않았습니다.
